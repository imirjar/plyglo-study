import 'package:poliglotim/app/app.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('Poliglotim app can be constructed', () {
    expect(const PoliglotimApp(), isA<PoliglotimApp>());
  });
}
